export const SET_MENU_ENABLED = (state, value) => { state.menuEnabled = value }
export const SET_USER = (state, user) => { state.user = user }
export const SET_TOKEN = (state, token) => { state.token = token }
export const SET_AUTHENTICATED = (state, value) => { state.authenticated = value }
export const SET_USERS = (state, users) => { state.users = users }
